# 1A2B 猜數字 — 規格書

本文件是本專案唯一的需求來源。沒有寫在這裡的功能，就不要做。

---

## 1. 專案設定

- 以 Vite + TypeScript（`vanilla-ts` 模板）建立，專案根目錄為 `guess-number/`
- **不引入** React、Vue 或任何 UI 框架
- **不引入** 任何外部遊戲或 UI 套件
- 測試工具使用 Vitest
- 桌機為主，但**手機與平板也要能好好玩**（見第 4 節「響應式」）
- 連線對戰使用 **Firebase 免費方案**（匿名登入、Firestore），不使用任何需要付費的服務
  - 只有連線對戰會用到；猜電腦模式完全離線可玩

---

## 2. 遊戲規則

### 2.1 共通規則

- 4 位數字，數字互不重複
- 首位可以是 0（例如 `0123` 為合法密碼）
- 猜測受同樣限制：4 碼、數字不重複
- 回饋格式為 `xAyB`
  - **A** = 數字對且位置對的個數
  - **B** = 數字對但位置錯的個數
- 猜到 `4A0B` 即猜中
- **沒有猜測次數上限**
- 非法輸入（非 4 碼、有重複數字、含非數字）由前端擋下，**不計次**
- 重複猜測已猜過的組合，擋下並提示，**不計次**

### 2.2 猜電腦：一般模式

- 開局時電腦隨機抽一組答案，之後固定不變

### 2.3 猜電腦：惡魔模式

- 電腦**不預先決定答案**，而是維護「所有與目前回饋一致的候選答案」
  - 開局時候選為全部 5040 組
- 玩家每猜一次：
  1. 把候選依「對這次猜測會得到哪種回饋」分組
  2. 保留**數量最多**的那一組，並回傳該組的回饋
  3. 數量相同的組以亂數挑選
  4. 只要還有其他組可選，**絕不挑 `4A0B` 那一組**
- 因此只有當候選只剩一組、且玩家剛好猜中那組時，才會得到 `4A0B`
- 放棄時揭曉的答案是剩餘候選中的一組；每一組都與至今所有回饋一致
- 參考數字：第一次猜 `0123`，最大組是 `0A1B`（1440 組），惡魔必定回 `0A1B`

### 2.4 暫停（僅猜電腦模式）

- 進行中可以暫停，暫停中可以繼續
- 暫停期間：
  - 計時停止，**暫停時間不計入用時**
  - 不接受任何猜測
  - 棋盤被蓋住（歷史紀錄、筆記板都看不到），避免暫停變成免費思考時間
- 已結束的局不能暫停
- 連線對戰**不提供**暫停

### 2.5 放棄

- **進行中隨時可以放棄**（包含開局還沒猜、以及暫停中）
- 放棄需**二次確認**，確認後揭曉答案
- 該局記為「放棄」，統計上與「勝利」**分開**

### 2.6 連線對戰

#### 房間

- 房主建立房間，取得 **6 位數字房號**
- 對手輸入房號加入；房間滿 2 人後不再接受加入
- 房間在建立 24 小時後自動清除
- 使用 Firebase 匿名登入；只有房間內兩位成員能讀寫該房間
- 對戰中重新整理頁面，可以回到同一場對戰（密碼暫存在該分頁的 sessionStorage，關閉分頁即清除）

#### 兩種對戰模式

**互相出題，同時猜**

- 雙方各自設定一組密碼（規則同 2.1），雙方都設定好才開始
- 同時開始猜對方的密碼，不需等待回合
- 可以看到對手對你的密碼猜了什麼、得到什麼回饋

**同一題比速度**（❌ 取消：需要 Cloud Functions 付費方案，不做）

- 伺服器出一題，雙方猜同一個答案
- 只看得到對手猜了幾次、是否已猜中，**看不到對手猜了什麼**（避免洩題）

#### 勝負（兩種模式相同）

- 雙方都猜中：**猜測次數少者勝**；次數相同，**用時短者勝**；再相同為平手
- 一方先猜中時，另一方繼續猜，直到：
  - 猜中 → 依上面規則比較
  - 放棄 → 判負
  - 已猜次數**達到**先猜中者的次數、仍未猜中 → 提前判負（下一次最快也會多一次）
- 放棄 = 判負；雙方都放棄 = 平手
- 斷線超過 60 秒視同放棄
- 對手送出的猜測超過 60 秒沒有被判定，視同負責判定的一方放棄（避免故意拖延）
- 以上兩種逾時，以及「結束後未公開密碼」，都由察覺的一方提出**申訴**、經伺服器驗證時間條件後才成立，雙方畫面的結果因此必定一致
- 用時 = 對戰開始到猜中那一次，**一律以伺服器時間戳記計算**
- 連線對戰**不提供**惡魔模式與暫停

#### 防作弊

- **資料庫永遠不存明文密碼或答案**
- 互相出題：
  - 資料庫只存 `SHA-256(密碼 + 32 位元組隨機鹽)`
  - 對手的猜測由密碼持有者的裝置判定並寫回回饋
  - 結束時雙方公開密碼與鹽，自動核對雜湊與過去**每一次**回饋
  - 核對失敗：說謊的一方判負
  - 結束後 60 秒內沒有公開密碼，視同核對失敗
- 同一題比速度（已取消）：
  - 答案只存放在 Cloud Functions 可讀、用戶端不可讀的位置
  - 每一次猜測都透過 callable function 由伺服器判定
  - 對戰結束前用戶端無法取得答案

---

## 3. 架構紀律

本專案要支援多種出題方式（一般、惡魔、連線對戰），因此出題邏輯必須可抽換。

1. 定義 `Codemaker` 介面，**只暴露三個方法**：
   - `startGame(config)`
   - `judge(guess)` → `{ A, B }`
   - `reveal()` → answer
2. 實作：
   - `LocalRandomCodemaker` — 一般模式
   - `DevilCodemaker` — 惡魔模式
   - 第二期 D：對手裝置判定的 Codemaker（互相出題）
   - ~~第二期 E：伺服器判定的 Codemaker（同一題比速度）~~ 已取消
3. 三個方法皆為**非同步**（回傳 `Promise`），以支援網路判定（第二期 C）
   - 方法仍然只有三個
   - 一局遊戲以 `await GameSession.create(codemaker)` 建立，建構子不對外開放
   - 等待判定期間再送出猜測，以 `'pending'` 擋下且不計次
   - 等待判定期間放棄，晚到的判定結果作廢
   - 判定失敗時，該組數字不計次、也不記為已猜過，可以重送；
     介面顯示「判定失敗，請再送出一次」
4. UI 層**只能**透過 `GameSession` 互動，**絕對不可以直接讀取答案**
5. 「一局遊戲」要是一個可以同時存在多份的資料結構（對戰時畫面上會有兩局），
   **不可以**用 `currentAnswer`、`guessHistory` 這類全域變數
6. 目錄結構：
   - `src/core/` — 純邏輯，**不得 import 任何 DOM API，也不得 import Firebase**
   - `src/online/` — 連線對戰（Firebase、需要網路的 Codemaker）
   - `src/ui/` — 介面層
   - `src/core/__tests__/` — 測試

---

## 4. 介面規格

### 整體

- 風格：明亮、清晰、留白足夠、圓角
- 淺色背景，**不要深色主題**
- 桌機（寬度 ≥1001px）外層最大寬度 **1280px 置中**，基準字 **18px**（第二期 G）
  - 左右留白至少 **32px**，隨視窗加寬到 **64px**，內容不要貼著螢幕邊緣
  - 模塊與字級相對原尺寸放大約 **1.1 倍**
  - 本節以下寫的尺寸是原尺寸，平板與手機沿用；桌機另依 1.1 倍放大
- 桌機遊戲畫面（猜電腦、連線對戰中）**固定在一個視窗內，不出現頁面捲軸**（第二期 G）
  - 視窗高度 ≥600px 時生效；更矮時改回整頁捲動
  - 歷史紀錄在自己的區塊內捲動；右欄內容放不下時在欄內捲動
  - 數字格、筆記格的高度隨視窗高度微調
  - 統計區改為三欄並排
- 版面任何寬度都**不可以出現水平捲動**
- 遊戲畫面為左右雙欄，左欄約 **1.45 份**、右欄 **1 份**，間距 **20px**（桌機 22px）
- 介面文字**全部使用繁體中文**

### 模式選擇畫面（第二期 B）

- 進入網站時先看到模式選擇
- 兩個主要選項：**猜電腦**、**連線對戰**
- 選「猜電腦」→ 選難度：**一般**、**惡魔**，各附一句說明
- 選「連線對戰」→ **建立房間**（再選對戰模式）或 **輸入房號加入**
  - 選單上「猜電腦」區塊直接列出「一般」「惡魔」兩張卡片，點卡片即開始；
    「連線對戰」區塊在它下方（第二期 D 起顯示）
- 結算覆蓋層除了「再玩一次」，另有「回到選單」

### 頂部

- 左邊：標題「1A2B 猜數字」
- 右邊：「第 N 次」與計時器
- 猜電腦模式在計時器旁有「暫停」按鈕（第二期 B），該局結束後隱藏
- 下方一條分隔線

### 左欄

**1. 輸入區**

- 4 個獨立數字格（高 **66px**）橫排，右側接一個「送出」按鈕
- 數字格**必須是 `<button>`，不可以是 `<input>`**
  - 這是手機體驗的關鍵：點 `<input>` 會叫出系統鍵盤擋住畫面，iOS 還會放大頁面
  - 點數字格只會切換「正在編輯的位置」，永遠不叫出系統鍵盤
- 實體鍵盤：
  - 打字自動跳下一格
  - Backspace 先清本格，本格已空則退回上一格並清掉
  - Enter 送出
  - 監聽掛在該局的容器上，不可掛在 `document`（對戰時要能分流）
- 頁面內建數字鍵盤（3 欄 × 4 列：1–9、刪除、0、送出）
  - 觸控裝置或窄視窗才顯示；顯示時隱藏行內的送出按鈕
- 目前聚焦的格子用**藍色外框加光暈**標示
- 下方一行灰色小字提示：「輸入 4 個不重複的數字，可用鍵盤直接打」
- 錯誤訊息**取代**該行提示，顯示紅色文字
- **數字重複**或**已猜過**被擋下時，四格自動全部清空、游標回到第一格
  （未填滿則保留已輸入的內容）

**2. 放棄**（第二期 F 起不在左欄）

- 左欄**不放**放棄按鈕
- 猜電腦：放棄在**暫停畫面**裡（見「暫停畫面」），進行中隨時可以按暫停再放棄
- 連線對戰：頂部計時器右側的「放棄」按鈕（見「④ 對戰中」）
- 兩者都需要二次確認；按鈕用**低調的外框樣式**，不要用紅色警示色

**3. 歷史紀錄**

- 緊接在輸入區下方，**最新在最上**
- 每列是一個淺灰圓角區塊，由左到右為：
  - `#編號`（灰色小字）
  - 4 個數字（**等寬字體、字距加寬**）
  - `xAyB`（**右對齊**，字級明顯放大、加粗）
- A 的部分用**綠色**，B 的部分用**橘色**
- `4A0B` 那列**整列高亮**
- 手機與平板**不在歷史區塊內做內捲**，讓整頁自然往下長；
  桌機固定畫面時改為在歷史區塊內捲動（第二期 G）

### 右欄

**1. 筆記板標題**

- 標題右側有「清除筆記」按鈕（第二期 G）
  - 一鍵清掉數字狀態列與位置推理表的全部標記，不需二次確認

**2. 數字狀態列**

- 0–9 共十格，排成 **5 欄 2 列**的正方形格子
- 點擊循環三種狀態：
  - **未定**（白底、深色外框）
  - → **排除**（灰底、灰字、刪除線）
  - → **必有**（淺綠底、深綠字）
  - → 回到未定
- 下方一行 **11px 灰色圖例**說明三種狀態

**3. 自動輔助**

- 當某次猜測結果為 `0A0B`，跳出提示詢問
  「要把這 4 個數字標記為排除嗎？」
- 按確認就自動標記

**4. 位置推理表**

- 以「第幾格」為基準（第二期 F）：**4 列（第 1–4 格）× 10 格（數字 0–9）**
  - 每列最左邊標示「第N格」，後面十個小格依序顯示數字 0–9
- 點擊小格，循環「這一格是不是這個數字」三種狀態：
  - **可能**（白底、深色外框、深色數字）
  - → **不可能**（灰底、灰字、刪除線）
  - → **確定**（淺綠底、深綠字）
  - → 回到可能
- 與上方數字狀態列**單向連動**（第二期 G）：
  - 數字在上方標為「排除」時，位置推理表上這個數字的四格自動變成「不可能」，
    顯示為淡色、點擊無效（0A0B 自動輔助標記的排除也一樣）
  - 取消排除後，恢復玩家原本自己標的狀態
  - 位置推理表的標記**不會**反過來改動數字狀態列
- 下方一行灰色圖例說明三種狀態
- **沒有自由打字的備註欄**

**5. 統計區**

- 上方一條分隔線
- 下面三行左右對齊的小字：
  - 總局數
  - 勝場
  - 最佳紀錄（最少次數）
- **一般與惡魔分開統計**，統計區顯示目前難度的數字（第二期 B）
  - 第二期之前累積的統計歸入「一般」
  - 分隔線下方以一行小字標示是哪個難度的統計（例如「惡魔模式統計」）

### 筆記狀態歸屬

筆記狀態（數字狀態列 + 位置推理表）屬於**「一局遊戲」的一部分**，新局開始時重置。

### 暫停畫面（第二期 B）

- 覆蓋層以**不透明背景**蓋住整個棋盤，顯示「已暫停」、「繼續」按鈕，
  以及下方外框樣式的「放棄這一局」按鈕（第二期 F）
- 按「放棄這一局」跳出二次確認，對話框疊在暫停畫面之上：
  - 「再想想」→ 回到暫停畫面
  - 「確定放棄」→ 揭曉答案，用時結算到暫停開始的那一刻
- 按「繼續」後，實體鍵盤可以立刻接著輸入，不需要再點一下數字格
- 暫停中計時器停止

### 響應式

| 尺寸 | 版面 |
| --- | --- |
| ≥1000px | 左右雙欄，最大寬度 1280px 置中，模塊與字級放大約 1.1 倍，遊戲畫面固定在一個視窗內 |
| <1000px | 單欄堆疊：輸入區 → 歷史紀錄 → 筆記板 |
| ≤720px | 單欄，字級與間距整體縮小一階 |

- 內建數字鍵盤的顯示條件：`(any-pointer: coarse)` 或視窗寬度 ≤720px
  - 也就是所有觸控裝置（含平板），以及被縮窄的桌機視窗
  - 顯示鍵盤時，錯誤訊息要排在數字格與鍵盤**之間**，不可被鍵盤推到看不見的地方
- 內建鍵盤最大寬度 420px，不隨面板拉滿
- 需要 `<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">`
- 覆蓋層（勝利、二次確認、暫停）在小螢幕上必須完整放得下
- 尊重 `prefers-reduced-motion`

### 結算畫面

以**覆蓋層**顯示：

- 猜中或放棄
- 花了幾次
- 用時多久（不含暫停）
- 答案
- 「再玩一次」按鈕
- 「回到選單」按鈕（第二期 B）

### 連線對戰畫面（第二期 D）

沿用單人模式的視覺與元件（數字格、內建鍵盤、歷史紀錄、筆記板），不另做一套風格。

**選單**

- 在「猜電腦」下方新增「連線對戰」區塊，兩張卡片：
  - **建立房間**：開一個房間，把房號告訴朋友
  - **加入房間**：輸入朋友給你的房號
- 只有「互相出題」一種對戰（同一題比速度已取消），建立房間時不需要選對戰模式

**① 等待對手（房主）**

```
          房號
      4 8 2 9 1 3
      [ 複製房號 ]
 把房號告訴朋友，等待對方加入…
        [ 取消 ]
```

- 房號大字、等寬字體、字距加寬
- 按「複製房號」後，按鈕文字暫時變成「已複製」
- 「取消」會關閉房間並回到選單
- 對手加入後自動進入 ③

**② 加入房間（對手）**

- 6 個數字格 + 內建數字鍵盤，操作方式與猜數字的輸入區相同
- 「加入」按鈕；另有「返回」回到選單
- 錯誤訊息取代提示行，顯示紅字：「找不到這個房號」「這個房間已經滿了」「這個房間已經過期」

**③ 設定密碼（雙方）**

```
 設定你的密碼
 對手要猜的就是這一組
 [ ][ ][ ][ ]   [ 確定 ]
 輸入 4 個不重複的數字
 ────────────────────
 對手：設定中…   →   對手：已設定 ✓
```

- 規則與猜測相同（4 碼、不重複），非法輸入不能確定
- 按「確定」後不能修改
- 雙方都確定後直接開始，不倒數

**④ 對戰中**

版面與單人模式相同，差異如下：

- 頂部：顯示「房號 482913」、「第 N 次」、計時器；**沒有暫停按鈕**，
  同一個位置改放「放棄」按鈕（需二次確認；自己猜中或放棄後隱藏）
- 左欄：輸入區、**我的**猜測紀錄
  - 送出後、對手裝置判定完成前，提示行顯示「判定中…」
- 右欄最上方新增「對手」面板，筆記板排在它下面，**不顯示統計區**：
  - 對手狀態：猜測中／已猜中／已放棄／連線中斷
  - 對手已猜幾次
  - 我的密碼（小字，提醒自己設了哪一組）
  - 對手對我的密碼的猜測紀錄，最新在上
- 手機單欄順序：輸入區 → 我的猜測紀錄 → 對手面板 → 筆記板
- 我先猜中時，輸入區換成「你猜中了！等待對手…」，對手面板繼續更新

**⑤ 結算**

- 大標：**你贏了**／**你輸了**／**平手**
- 一行勝負原因，例如「次數較少」「次數相同，用時較短」「對手放棄」「對手連線中斷」「對手回報的結果不實」
- 雙方對照：

  | | 你 | 對手 |
  | --- | --- | --- |
  | 猜測次數 | 5 次 | 7 次 |
  | 用時 | 02:14 | 03:40 |
  | 密碼 | 4721 | 0856 |

- 一行核對結果：「雙方回饋核對無誤」，或指出哪一方回報不實
- 按鈕只有「回到選單」，不做再戰

---

## 5. 分階段執行

一次只做一個階段，每階段做完停下來等確認，不要自己往下做。

### 第一期（已完成）

| 階段 | 內容 | 狀態 |
| --- | --- | --- |
| Phase 1 | `src/core/` 純邏輯 + 完整單元測試，零 UI | ✅ 完成 |
| Phase 2 | 最小可玩 UI（能輸入、能顯示歷史、能判勝） | ✅ 完成 |
| Phase 3 | 輸入驗證、重複猜測偵測、放棄功能 | ✅ 完成 |
| Phase 4 | 筆記板完整功能 | ✅ 完成 |
| Phase 5 | 視覺完稿與 localStorage 統計 | ✅ 完成 |

### 第二期

| 階段 | 內容 | 需要後端 | 狀態 |
| --- | --- | --- | --- |
| A | 單人核心：惡魔模式、暫停計時、隨時放棄 + 測試 | 否 | ✅ 完成 |
| B | 單人介面：模式選擇、難度選擇、暫停畫面、依難度分開統計、回到選單 | 否 | ✅ 完成 |
| C | `Codemaker` 非同步化（行為不變，測試全過） | 否 | ✅ 完成 |
| D | 連線對戰：房間、互相出題同時猜（Firestore + 雜湊防作弊） | Firebase | ✅ 完成 |
| E | 連線對戰：同一題比速度（Cloud Functions 裁判） | Firebase Blaze | ❌ 取消（需付費方案） |
| F | 介面調整：桌機放大約 1.2 倍、放棄移到暫停畫面／對戰頂部、位置推理改以格為基準 | 否 | ✅ 完成 |
| G | 介面調整：桌機加大留白並稍微縮小（約 1.1 倍）、遊戲畫面固定不捲動、排除數字連動位置推理、一鍵清除筆記 | 否 | ✅ 完成 |

---

## 6. 測試要求

### 第一期 Phase 1

- `judge('1234', '1234')` → `4A0B`
- `judge('1234', '4321')` → `0A4B`
- `judge('1234', '1243')` → `2A2B`
- `judge('1234', '5678')` → `0A0B`
- 產生的答案永遠是 4 碼且無重複數字
- 連續產生 1000 組答案，全部合法
- `A + B` 永遠不超過 4
- **不可能產生 `3A1B`**（關鍵的邊界測試）

### 第二期 A

- 惡魔模式：
  - 第一次猜 `0123` 必定回 `0A1B`
  - 每次回傳的都是當下最大分組的回饋
  - 候選多於一組時，絕不回傳 `4A0B`
  - 揭曉的答案與至今所有回饋一致
  - 「每次猜目前還可能的第一組」的玩家一定會贏
  - 相同亂數種子行為完全相同；兩個實例互不干擾；內部狀態不可列舉
- 暫停：
  - 暫停中猜測被擋下且不計次
  - 暫停時間不計入用時；多次暫停累計扣除
  - 暫停中放棄，用時結算到暫停開始的那一刻
  - 已結束的局不能暫停；兩局的暫停互不影響
- 放棄：
  - 開局還沒猜就可以放棄
  - 獲勝後不能放棄；放棄後不能反悔

### 第二期 C

- 兩種出題者的三個方法都回傳 `Promise`；錯誤以拒絕（reject）表示
- `GameSession.create` 會等 `startGame` 完成才回傳
- 判定還沒回來時再送出，以 `'pending'` 擋下，且不會送出第二次判定
- 判定期間放棄：晚到的結果作廢，就算是 `4A0B` 也不會變成獲勝
- 判定失敗：例外往外拋、不計次、不記為已猜過，可以重送
- 判定期間暫停：結果回來仍會記錄
- 兩局可以同時等待判定，互不阻擋
- 第一期與第二期 A 的所有測試改為非同步後全部通過

### 第二期 D（草案）

- 勝負推導（純函式，不依賴 Firebase）：
  - 雙方都猜中：次數少者勝；同次數比用時；再相同平手
  - 一方先猜中，另一方次數達到仍未猜中：提前判負
  - 放棄、連線中斷、判定逾時：判負；雙方都放棄：平手
- 雜湊與核對：
  - 同一組密碼搭配不同的鹽，雜湊不同
  - 公開的密碼與雜湊不符 → 核對失敗
  - 任何一次回饋與 judge(公開的密碼, 猜測) 不符 → 核對失敗，並指出是哪一方
- 安全規則（Firebase 模擬器）：
  - 非房間成員讀不到房間
  - 不能修改自己已寫入的雜湊
  - 不能替對方判定、不能判定兩次、不能寫入不合法的 A、B
  - 不合法的猜測、重複的猜測、替別人公開密碼都被擋下
  - 申訴只有在伺服器驗證的時間條件成立時才被接受，且寫入後不可修改

---

## 7. 其他要求

- 專案根目錄需有 `SPEC.md`（本檔）與 `CLAUDE.md`
- 介面文字全部使用繁體中文

---

## 8. 連線對戰技術設計（第二期 D）

### 資料結構（Firestore）

```
rooms/{房號}
  mode          'mutual'（E 階段加入 'race'）
  hostUid / guestUid
  createdAt     伺服器時間
  expiresAt     建立後 24 小時，由 Firestore TTL 自動刪除

rooms/{房號}/players/{uid}
  commitHash    SHA-256(密碼:鹽)，寫入後不可修改
  committedAt   寫入雜湊的伺服器時間
  lastSeen      每 20 秒更新一次的伺服器時間（判斷斷線用）
  surrendered   是否放棄；放棄後不能反悔
  expiresAt     與房間相同，讓 TTL 一併清除

rooms/{房號}/guesses/{猜的人_猜測}
  from / to     猜的人 / 密碼持有者
  index         這位玩家第幾次猜
  guess
  createdAt     送出時的伺服器時間（用時以此計算，對手判定慢不會拖累你的用時）
  feedback      密碼持有者寫回的 {A, B}，寫入後不可修改
  judgedAt
  expiresAt

rooms/{房號}/reveals/{uid}
  code / salt   公開的密碼與鹽，寫入後不可修改
  createdAt     公開時的伺服器時間
  expiresAt

rooms/{房號}/claims/{類型_被申訴者}
  type          'disconnect' | 'judge-timeout' | 'no-reveal'
  claimant / against
  guessId       判定逾時時，是哪一筆猜測沒有被判定
  createdAt / expiresAt
```

- 房間**沒有「狀態」欄位**：等待中、設定密碼中、對戰中、已結束，全部由上面的資料推導，
  避免雙方各自寫入狀態而互相衝突
- 對戰開始時間 = 雙方 `committedAt` 中較晚的那一個
- 猜測的文件 ID 固定為「猜的人_猜測」，資料庫層級就不可能重複猜同一組
- 斷線、判定逾時、未公開密碼這三種逾時**不依賴各自的時鐘**：由察覺的一方寫入申訴，伺服器以安全規則驗證時間條件後才接受；申訴不可修改，雙方都依它判斷勝負

### 流程

1. 房主匿名登入，亂數產生房號並建立房間；房號已被使用就重抽
2. 對手輸入房號，以交易寫入 guestUid；已經有人則回報「已滿」
3. 雙方在本機產生 32 位元組的隨機鹽，只把雜湊寫進資料庫；密碼與鹽留在本機
4. 雙方都寫入雜湊後即開始，開始時間取雙方寫入雜湊時間中較晚的一個
5. 猜測：寫入一筆 guesses → 密碼持有者的裝置收到後**自動**判定並寫回 feedback
6. 勝負由雙方裝置依相同規則、相同資料各自推導，結果一致
7. 結束後雙方寫入 reveals，各自核對對方的雜湊與過去每一次回饋

### 程式結構

- `src/core/`：新增「依雙方資料推導勝負」與「核對回饋」的純函式，可完整單元測試
- `src/online/firebase.ts`：初始化 Firebase（設定值是公開識別碼，可以進版控）
- `src/online/room.ts`：建房、加入、監聽房間、心跳
- `src/online/commitment.ts`：產生鹽、計算雜湊（Web Crypto）
- `src/online/OpponentCodemaker.ts`：
  - `judge()`：寫入猜測，等對手裝置寫回結果
  - `reveal()`：等對方公開密碼
- `src/ui/`：連線選單卡片、等待畫面、加入畫面、設定密碼畫面、對手面板、對戰結算

### 安全規則重點

- 只有登入（匿名）的使用者可以存取
- 房間與子集合只有房主與對手可以讀寫
- 只能寫自己的 `players.{uid}`；`commitHash` 寫入後不可修改
- `guesses`：只能以自己的身分建立、只能猜對方、雙方都設定好密碼才能猜、猜測必須合法（4 碼不重複）、文件 ID 必須是「猜的人_猜測」、建立時 `feedback` 必須為空
  - 為什麼要擋不合法的猜測：誠實的一方無法判定不合法的猜測；若不擋，作弊者可以藉此讓對方被判「判定逾時」
- `feedback`：只有密碼持有者能寫、只能寫一次；A、B 為 0–4 的整數且 A + B ≤ 4，也不可能是 3A1B
- `reveals`：只有本人能寫、寫入後不可修改
  - 不限定「結束後」才能寫：提早公開只會讓對手知道自己的密碼，傷害的是自己，不需要用規則擋
- `claims`：只能以自己的名義申訴對手，寫入後不可修改；伺服器驗證的條件：
  - 斷線：對手最後一次心跳超過 60 秒
  - 判定逾時：自己的某筆猜測送出超過 60 秒，對手仍未判定
  - 未公開密碼：自己已經公開超過 60 秒，對手仍未公開
- 以 Firebase 模擬器在 GitHub Actions 上測試（本機不需要 Java）；規則測試沒過就不部署

### 建置與部署的調整

- 新增執行期依賴 `firebase`
- 連線模組**按需載入**，只玩猜電腦的人不會下載 Firebase；
  因此部署改用 Vite 標準的多檔輸出，不再打包成單一 HTML
- 新增 `firebase.json`、`.firebaserc`、`firestore.rules`；安全規則用 `firebase deploy` 部署，**部署前會先徵求同意**
- 建議到 Google Cloud 主控台，把 API key 限制為只能從 `jstsai94.github.io` 與 `localhost` 使用
