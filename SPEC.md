# 2A2B 猜數字 — 規格書

本文件是本專案唯一的需求來源。沒有寫在這裡的功能，就不要做。

---

## 1. 專案設定

- 以 Vite + TypeScript（`vanilla-ts` 模板）建立，專案根目錄為 `guess-number/`
- **不引入** React、Vue 或任何 UI 框架
- **不引入** 任何外部遊戲或 UI 套件
- 測試工具使用 Vitest
- 只做桌機版，**不做手機適配、不做 RWD、不做斷點**

---

## 2. 遊戲規則

- 4 位數字，電腦出題，玩家猜
- 答案的 4 個數字互不重複
- 首位可以是 0（例如 `0123` 為合法答案）
- 玩家的猜測受同樣限制：4 碼、數字不重複
- 回饋格式為 `xAyB`
  - **A** = 數字對且位置對的個數
  - **B** = 數字對但位置錯的個數
- 猜到 `4A0B` 獲勝
- **沒有猜測次數上限**
- 第 10 次猜測結束後，才出現「放棄」按鈕
  - 在此之前這個按鈕**不存在**（不是 disabled，是不渲染）
- 放棄需**二次確認**，確認後揭曉答案
  - 該局記為「放棄」，統計上與「勝利」**分開**
- 非法輸入（非 4 碼、有重複數字、含非數字）由前端擋下，**不計次**
- 重複猜測已猜過的組合，擋下並提示，**不計次**

---

## 3. 架構紀律

本專案未來要擴充「惡魔模式」與「雙人對戰」，因此出題邏輯必須可抽換。

1. 定義 `Codemaker` 介面，**只暴露三個方法**：
   - `startGame(config)`
   - `judge(guess)` → `{ A, B }`
   - `reveal()` → answer
2. 本次只實作 `LocalRandomCodemaker`（開局隨機抽一組答案）
3. UI 層**只能**透過 `Codemaker` 介面互動，**絕對不可以直接讀取答案**
4. 「一局遊戲」要是一個可以同時存在多份的資料結構（未來對戰時畫面上會有兩局），
   **不可以**用 `currentAnswer`、`guessHistory` 這類全域變數
5. 目錄結構：
   - `src/core/` — 純邏輯，**不得 import 任何 DOM API**
   - `src/ui/` — 介面層
   - `src/core/__tests__/` — 測試

---

## 4. 介面規格（桌機版）

### 整體

- 風格：明亮、清晰、留白足夠、圓角
- 淺色背景，**不要深色主題**
- 外層**固定寬度 960px 置中**，不做流體佈局、不做斷點
- 版面為左右雙欄，左欄約 **1.45 份**、右欄 **1 份**，間距 **20px**
- 介面文字**全部使用繁體中文**

### 頂部

- 左邊：標題「2A2B 猜數字」
- 右邊：「第 N 次」與計時器
- 下方一條分隔線

### 左欄

**1. 輸入區**

- 4 個獨立數字格（高 **52px**）橫排，右側接一個「送出」按鈕
- 鍵盤優先：
  - 打字自動跳下一格
  - Backspace 退回上一格
  - Enter 送出
- 目前聚焦的格子用**藍色外框加光暈**標示
- 下方一行灰色小字提示：「輸入 4 個不重複的數字，可用鍵盤直接打」
- 錯誤訊息**取代**該行提示，顯示紅色文字

**2. 放棄按鈕**

- 第 10 次猜測後才出現在送出按鈕下方
- 用**低調的外框樣式**，不要用紅色警示色

**3. 歷史紀錄**

- 緊接在輸入區下方，**最新在最上**
- 每列是一個淺灰圓角區塊，由左到右為：
  - `#編號`（灰色小字）
  - 4 個數字（**等寬字體、字距加寬**）
  - `xAyB`（**右對齊**）
- A 的部分用**綠色**，B 的部分用**橘色**
- `4A0B` 那列**整列高亮**
- **不要在歷史區塊內做內捲**，讓整頁自然往下長

### 右欄

**1. 筆記板標題**

**2. 數字狀態列**

- 0–9 共十格，排成 **5 欄 2 列**的正方形格子
- 點擊循環三種狀態：
  - **未定**（白底、深色外框）
  - → **排除**（灰底、灰字、刪除線）
  - → **必有**（淺綠底、深綠字）
  - → 回到未定
- 下方一行 **11px 灰色圖例**說明三種狀態

**3. 自動輔助**

- 當某次猜測結果為 `0A0B`，跳出提示詢問
  「要把這 4 個數字標記為排除嗎？」
- 按確認就自動標記

**4. 自由備註**

- 一個 `textarea`，高度約 **62px**
- placeholder 為「解題筆記…」

**5. 統計區**

- 上方一條分隔線
- 下面三行左右對齊的小字：
  - 總局數
  - 勝場
  - 最佳紀錄（最少次數）

### 筆記狀態歸屬

筆記狀態（數字標記 + 自由備註）屬於**「一局遊戲」的一部分**，新局開始時重置。

### 勝利畫面

以**覆蓋層**顯示：

- 花了幾次
- 用時多久
- 答案
- 「再玩一次」按鈕

---

## 5. 分階段執行

五個階段皆已完成。

| 階段 | 內容 | 狀態 |
| --- | --- | --- |
| Phase 1 | `src/core/` 純邏輯 + 完整單元測試，零 UI | ✅ 完成 |
| Phase 2 | 最小可玩 UI（能輸入、能顯示歷史、能判勝） | ✅ 完成 |
| Phase 3 | 輸入驗證、重複猜測偵測、放棄功能 | ✅ 完成 |
| Phase 4 | 筆記板完整功能 | ✅ 完成 |
| Phase 5 | 視覺完稿與 localStorage 統計 | ✅ 完成 |

---

## 6. Phase 1 測試要求

必須包含以下項目：

- `judge('1234', '1234')` → `4A0B`
- `judge('1234', '4321')` → `0A4B`
- `judge('1234', '1243')` → `2A2B`
- `judge('1234', '5678')` → `0A0B`
- 產生的答案永遠是 4 碼且無重複數字
- 連續產生 1000 組答案，全部合法
- `A + B` 永遠不超過 4
- **不可能產生 `3A1B`**（關鍵的邊界測試）

---

## 7. 其他要求

- 專案根目錄需有 `SPEC.md`（本檔）與 `CLAUDE.md`
- 介面文字全部使用繁體中文
