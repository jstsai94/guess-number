# 1A2B 猜數字 — 專案總結

整理日期：2026-09-17　／　狀態：第二期 A–I 完成並上線（E 取消）

這份文件是「這個專案到目前為止長什麼樣、為什麼長成這樣」的總覽。
規格的唯一來源仍然是 [SPEC.md](./SPEC.md)，工作守則見 [CLAUDE.md](./CLAUDE.md)；
這裡補的是它們沒寫的東西：決策的理由、討論的過程、踩過的坑。

---

## 1. 一分鐘摘要

- 4 位不重複數字的 1A2B 猜數字遊戲，繁體中文介面，桌機為主、手機平板也能好好玩。
- 線上版：<https://jstsai94.github.io/guess-number/>（公開、免登入、push 到 `main` 自動部署）。
- 三種玩法：**單機模式**（一般／惡魔／電腦解題）與**連線模式**（房號對戰，互相出題同時猜）。
- 技術：Vite + TypeScript（vanilla-ts），沒有 UI 框架；唯一的執行期依賴是 Firebase，
  而且只有進入連線模式時才會下載。
- 測試：單元測試 176 項（10 個檔案）＋ Firestore 安全規則測試 24 項，CI 沒過就不部署。
- 程式規模：`src/` 約 7000 行 TypeScript，安全規則 215 行。

---

## 2. 現有功能與規格摘要

### 2.1 共通規則

- 答案 4 碼、數字互不重複，首位可以是 0。
- 回饋 `xAyB`：A = 數字對且位置對，B = 數字對但位置錯；猜到 `4A0B` 獲勝。
- 沒有次數上限。非法輸入（長度不符、重複數字、非數字）與重複猜測都會被擋下且**不計次**。
- 數字重複或已猜過被擋下時，四格自動清空、游標回第一格。

### 2.2 單機模式

| 玩法 | 內容 |
| --- | --- |
| 一般 | 開局隨機抽一組答案，之後固定不變 |
| 惡魔 | 不預先決定答案，每次保留「與所有回饋一致」的最大分組，只要還有別組就絕不回 `4A0B` |
| 電腦解題 | 玩家出題給電腦猜，電腦用「最壞情況最少」策略，每一步附推理說明 |

- **暫停**：只有猜電腦有。暫停時計時停止、棋盤被完全蓋住（避免變成免費思考時間）。
- **放棄**：進行中隨時可以放棄，放在暫停畫面裡，需要二次確認。
  猜不到 2 次（0 或 1 次）就放棄的局不列入統計。
- **筆記板**：上半部是 0–9 數字狀態列（未定／排除／必有），
  下半部是 4 列（第 1–4 格）× 10 格（數字 0–9）的位置推理表（可能／不可能／確定）。
  數字標為「排除」時，位置推理表上這個數字四格自動變成不可能且點不動；取消排除會還原。
  標題列有「清除筆記」一鍵清空。猜到 `0A0B` 會問要不要自動標記排除。
- **統計**：一般與惡魔分開計算，存在瀏覽器裡。最佳紀錄＝次數最少的勝場，
  次數相同比用時，顯示成「4 次 · 01:23」。

### 2.3 電腦解題的策略與說明

- 每次從全部 5040 組裡挑「各種回饋中剩最多的那一種，組數最少」的一組（最壞情況最少）。
  同分時優先挑本身還可能是答案的，再比平方和，最後取數字最小的。
- 第一次固定猜 `0123`（開局所有猜法等價）；解題過程是固定的，同一組密碼每次都一樣。
- 已驗證：**全部 5040 組密碼都在 7 次內猜中，平均不到 5.6 次**。
- 每一步的說明分兩段：
  - **為什麼猜這組**：猜之前剩幾組、每個數字的角色（主要測試／測試位置／放原位／只佔位置）、
    這組本身可不可能是答案（不可能時比較「只從可能答案裡挑」的最壞情況）、
    每種回饋剩幾組（最壞橘字、實際藍框）、最壞與平均。
  - **這次回饋推出了什麼**：直接讀出的資訊，以及結合過去回饋後**新**確定的結論。
- 節奏：下一步／自動播放（1.5 秒一步）／看完整過程，點任一列可回看那一步。

### 2.4 連線模式（互相出題，同時猜）

- 房主建立房間拿到 6 位房號，對手輸入房號加入，滿 2 人後不再接受加入，房間 24 小時自動清除。
- 雙方各設一組密碼，都設定好就開始；可以看到對手猜了什麼、得到什麼回饋。
- 勝負：先比猜測次數，次數相同比用時（伺服器時間戳記），再相同為平手。
  一方先猜中後，另一方次數**達到**先猜中者仍未猜中就提前判負。
- 放棄＝判負（按鈕在頂部，位置同單機的暫停）；雙方都放棄為平手。
- 斷線超過 60 秒、或超過 60 秒沒判定對手的猜測，視同放棄 —— 但要由察覺的一方提出**申訴**，
  經安全規則以伺服器時間驗證後才成立，所以雙方畫面的結果必定一致。
- 防作弊：資料庫永遠不存明文密碼，只存 `SHA-256(密碼 + 32 位元組隨機鹽)`；
  結束時雙方公開密碼與鹽，自動核對雜湊與**每一次**回饋，說謊的一方判負。

### 2.5 介面與響應式

| 尺寸 | 版面 |
| --- | --- |
| ≥1001px（桌機） | 左右雙欄，最大寬度 1280px 置中、左右留白 32–64px，基準字 18px；遊戲畫面固定在一個視窗內不捲動 |
| 721–1000px | 單欄堆疊 |
| ≤720px（手機） | 單欄，字級與間距縮小一階 |

- 桌機固定畫面：歷史紀錄在自己的區塊內捲動，格子高度隨視窗高度微調，統計區三欄並排；
  視窗高度 <600px 時放棄固定、改回整頁捲動。
- 觸控裝置（`any-pointer: coarse`）或 ≤720px 顯示頁面內建數字鍵盤。
- 輸入用的數字格一律是 `<button>`，**永遠不能改回 `<input>`**，否則觸控裝置會跳出系統鍵盤。
- 不做深色主題。

### 2.6 資料保存方式

沒有帳號，所有資料都在「這個瀏覽器」裡：

| 資料 | 存在哪裡 | 保存多久 |
| --- | --- | --- |
| 統計（局數、勝場、最佳紀錄） | localStorage | 一直保留，清除網站資料就沒了 |
| 連線模式的匿名身分 | 瀏覽器（Firebase 匿名登入） | 同上，只用來進房間 |
| 對戰時自己的密碼 | 該分頁的 sessionStorage | 關掉分頁就清除 |
| 對戰房間 | Firestore | 建立 24 小時後清除 |

換裝置、換瀏覽器、無痕視窗都是各自獨立的，伺服器上沒有任何個人紀錄。

---

## 3. 技術架構

### 3.1 目錄職責

```
src/
├─ core/        純邏輯：禁止 DOM API、禁止 Firebase
│  ├─ Codemaker.ts / LocalRandomCodemaker.ts / DevilCodemaker.ts
│  ├─ judge.ts / codeGenerator.ts / types.ts
│  ├─ GameSession.ts / GameNotes.ts
│  ├─ solver.ts        電腦解題（不是 Codemaker）
│  ├─ versus.ts        連線對戰勝負判定與回饋核對（純函式）
│  └─ __tests__/
├─ online/      Firebase：房間、commit–reveal、對手判定的 Codemaker、對戰控制器
├─ ui/          介面層，只從 core/index.ts 匯入
│  └─ versus/   連線對戰畫面（整包按需載入）
└─ main.ts
```

### 3.2 四條架構紀律（一直沒有破例）

1. **出題邏輯可抽換**：`Codemaker` 只有 `startGame` / `judge` / `reveal` 三個方法，
   一般、惡魔、對手裝置判定各自實作，`GameSession` 與 UI 都不必改。
2. **UI 拿不到答案**：UI 只拿得到 `GameSession`，遊戲進行中 `getAnswer()` 一律是 `null`，
   答案存在 JS 私有欄位（`#answer`、`#candidates`），`as any` 也讀不到。
3. **一局＝一個 `GameSession` 實例**：沒有 `currentAnswer` 這種模組層級狀態，
   所以連線對戰能在同一頁同時存在兩局。
4. **core 不知道 UI 與網路的存在**：錯誤用代碼（`RejectReason`）表示，中文訊息由 UI 翻譯。

### 3.3 Firebase

- 專案 `guess-number-ae8ee`，只用**免費方案**：匿名登入 + Firestore，沒有 Cloud Functions。
- 安全規則（215 行）負責：房間建立／加入／取消、密碼雜湊只能寫一次、放棄不可逆、
  猜測必須合法（RE2 逐位檢查、擋掉不可能的 `3A1B`）、公開密碼不可竄改、申訴的時間條件驗證。
- 用戶端拿到的只有公開的 `firebaseConfig`；服務帳號金鑰從未進入專案。

### 3.4 建置與部署

- push 到 `main` → GitHub Actions 三個工作：測試與建置、安全規則測試（Firestore 模擬器 + Java）、
  部署到 GitHub Pages。任何一項沒過就不部署。
- 產出大小（最近一次）：主程式 JS 約 34 KB、CSS 約 18 KB；
  連線模組 561 KB（含 Firebase）**只有進對戰才下載**。
- `dist/` 是建置產物，不進版控。

---

## 4. 開發歷程與決策紀錄

### 4.1 第一期（2026-09-12）

| 階段 | 內容 |
| --- | --- |
| Phase 1 | `src/core/` 純邏輯 + 完整單元測試，零 UI |
| Phase 2 | 最小可玩 UI |
| Phase 3 | 輸入驗證、重複猜測偵測、放棄 |
| Phase 4 | 筆記板完整功能 |
| Phase 5 | 視覺完稿與 localStorage 統計 |

同一天接著做的三件事：

- **介面調整**：遊戲正名為 1A2B、整體放大、重複數字自動清空、
  筆記從打字備註改成「用格子模擬位置」的位置推理表。
- **手機與平板**：數字格改成 `<button>` + 頁面內建數字鍵盤，解決系統鍵盤擋住畫面的問題。
- **自動部署**：改用 GitHub Actions，push 就上線。

**你的原則**（後來一直沿用）：規則與介面要符合當初的要求；盡量用新增檔案的方式擴充、
少改既有檔案；專案保持獨立；成果要是一個「任何電腦打開就能玩、不用登入」的連結。

### 4.2 第二期（2026-09-14 ～ 09-15）

| 階段 | 內容 | 狀態 |
| --- | --- | --- |
| A | 惡魔模式、暫停計時、隨時放棄（核心邏輯 + 測試） | ✅ |
| B | 模式選擇、難度選擇、暫停畫面、依難度分開統計、回到選單 | ✅ |
| C | `Codemaker` 非同步化（行為不變，測試全過） | ✅ |
| D | 連線對戰：房間、互相出題同時猜（Firestore + 雜湊防作弊） | ✅ |
| E | 同一題比速度（Cloud Functions 裁判） | ❌ 取消 |
| F | 桌機放大約 1.2 倍、放棄移入暫停、位置推理改以格為基準 | ✅ |
| G | 桌機加大留白並縮回約 1.1 倍、固定畫面、排除連動、一鍵清除筆記 | ✅ |
| H | 改名單機／連線模式、新增電腦解題 | ✅ |
| I | 卡片移除說明、最佳紀錄含用時、太早放棄不計、「為什麼猜這組」 | ✅ |

### 4.3 過程中的關鍵決定

- **對戰要比什麼**：先比猜測次數，再比花費時間。（你的決定）
- **後端選 Firebase**：匿名登入 + Firestore 就能做到免登入、跨裝置連線，且在免費額度內。
- **放棄的時機**：隨時都能放棄，包含開局還沒猜、以及暫停中。（你的決定）
- **E 取消**：同一題比速度需要 Cloud Functions，也就是要升級到 Blaze 付費方案；
  你決定不付費，所以整個專案維持免費方案，之後也不要再加需要付費的功能。
- **放棄按鈕的位置**（F）：從左欄移到暫停畫面裡（猜電腦）與頂部（連線對戰，因為對戰沒有暫停）。
- **筆記改以「第幾格」為基準**（F）：思考時是想「第 2 格不可能是 5、7」，
  比原本以數字為基準的 10×4 表格更貼近實際推理；資料結構沒變，只換顯示方式。
- **畫面固定不捲動**（G）：為此把歷史紀錄改成在區塊內捲動，
  這一點和第一期「不要在歷史區塊內做內捲」的規格衝突，已在 SPEC 改成分尺寸描述。
- **電腦解題的四個細節**（H，你逐項選的）：名字叫「電腦解題」、策略用「最壞情況最少」、
  幾A幾B由程式自動判定、說明先只寫「這次回饋推出了什麼」。
- **再加上「為什麼猜這組」**（I）：因為只知道排除了什麼還不夠，
  你想學的是「下一組怎麼猜比較有效率」，所以把電腦挑猜法的理由也攤開來講。

### 4.4 D 階段設計審查時修掉的三個設計缺陷

這三個是寫成程式**之前**發現的，值得記著：

1. **用時鐘判斷逾時會讓兩邊各說各話**：改成「由察覺的一方提出申訴，
   安全規則用 `request.time` 驗證時間條件」，結果才會一致。
2. **非法猜測可以陷害對手**：如果對手送出不合法的猜測，誠實的一方無法判定，
   反而會被判「判定逾時」；改成安全規則直接擋掉不合法的猜測。
3. **提前判負的條件寫錯**：草案寫「超過」先猜中者的次數，
   正確應該是「**達到**」就判負（下一次最快也會多一次）。

### 4.5 實作過程中修掉的問題

- **壓縮器輸出舊瀏覽器看不懂的 CSS**：鎖定 `build.cssTarget`，
  否則舊 Safari 會整段丟掉媒體查詢，觸控裝置就沒有輸入方法。
- **「加入」按鈕文字被擠成直排**：房號 6 格比猜數字多 2 格，需要縮小格子並禁止換行。
- **`permission-denied` 的錯誤訊息會誤導玩家**：改成「對戰伺服器還沒有設定完成」。
- **電腦解題卡在「思考中…」**：原本用 `requestAnimationFrame` 等畫面更新再開始計算，
  但分頁在背景時不會更新畫面，就會一直等下去；改用計時器。
- **推理說明重複**：已確定在某一格的數字，不必再列「第 1 格不可能是它」。

---

## 5. 測試概況

| 範圍 | 項數 | 重點 |
| --- | --- | --- |
| `judge` / `codeGenerator` | 38 | `3A1B` 不可能、A+B ≤ 4、產生的答案永遠合法 |
| `GameSession` | 41 | 暫停不計時、判定期間放棄／暫停、pending 擋重複送出 |
| `GameNotes` | 18 | 兩套標記、排除連動位置推理、reset |
| `LocalRandomCodemaker` / `DevilCodemaker` | 28 | 惡魔永遠保留最大分組、絕不提早給 `4A0B` |
| `solver`（電腦解題） | 18 | 全 5040 組決策樹檢查、最壞情況最小、每條結論都與密碼相符 |
| `versus`（勝負判定） | 22 | 次數／時間／放棄／斷線／作弊各種組合 |
| `online`（雜湊、對手判定） | 11 | commit–reveal、等待對手回應 |
| Firestore 安全規則 | 24 | 房間、密碼、猜測、公開、申訴的權限邊界 |

單元測試共 **176 項**，安全規則測試 **24 項**，都在 CI 執行。

---

## 6. 待辦與未來可選項

**建議但還沒做的：**

- 到 Google Cloud 主控台把 Firebase API 金鑰限制在 `jstsai94.github.io`、`localhost:5180`、
  `guess-number-ae8ee.firebaseapp.com`（不影響遊玩，只是避免別的網站盜用金鑰）。
- README 有幾處已經過期：放棄按鈕的說明、位置推理表的方向（現在是 4×10）、測試項數、
  「LocalRandomCodemaker 是唯一實作」。要更新跟我說。

**討論過但沒有做的：**

- 跨裝置同步統計。兩種免費做法：匯出／匯入代碼（不用登入、手動），
  或 Google 登入 + Firestore（自動，但多一道登入，可設計成選用）。
- 同一題比速度（第二期 E）：需要付費方案，已取消。

---

## 7. 重新上手須知

```bash
npm install
npm run dev        # 開發伺服器（port 5180）
npm run test:run   # 單元測試
npm run typecheck  # 型別檢查
npm run build      # 建置到 dist/
npm run test:rules # 安全規則測試（需要 Firestore 模擬器與 Java，平常交給 CI）
```

- 改完 push 到 `main` 就會自動部署，不必在本機跑任何部署指令。
- 動手前先看 [CLAUDE.md](./CLAUDE.md)：不要在沒被要求時重構、不要做 SPEC 沒寫的功能、
  一次只做一個階段。
- 任何規格變動都要同步更新 [SPEC.md](./SPEC.md)，它是唯一的需求來源。
